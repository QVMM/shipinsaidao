# 质量验证规范

本项目按真实产品交付标准验证。发布前必须完成静态检查、单元测试、生产构建、端到端测试和视觉回归；任一步失败都不得交付。

## 一键验证

```bash
npm run test:all
```

## 分层测试

| 层级 | 命令 | 覆盖范围 | 通过标准 |
| --- | --- | --- | --- |
| 静态检查 | `npm run lint` | 前端、服务端、单元测试、端到端测试 | 0 error |
| 单元测试 | `npm run test:unit` | 判定逻辑、报告门禁、离线模式、AI 证据约束、产品文案 | 全部通过 |
| 生产构建 | `npm run build` | Vite 生产资源、模块依赖、样式编译 | 构建成功 |
| 功能回归 | `npm run test:e2e` | 登录选批、全部业务页面、大屏、AI 会话、全屏研判、响应式、控制台异常 | 两种窗口尺寸全部通过 |
| 视觉回归 | `npm run test:visual` | 指挥舱与健康评价关键页面 | 与已审核基线一致 |

## 浏览器矩阵

自动化使用本机 Chrome，覆盖：

- 1440 × 900：常规桌面工作台。
- 1041 × 1001：紧凑窗口与现场分屏环境。
- `prefers-reduced-motion`：验证减少动态效果时仍可完整操作。

## 关键验收项

- 大屏批次队列保持稳定，不自动位移或截断首行。
- 页面无横向溢出，无浏览器控制台错误。
- AI 回答引用当前批次证据；证据不足时明确拒绝下结论。
- 会话可以新建、清空；全屏研判可以进入和退出。
- 健康评价只存在一套 AI 助手。
- 所有业务页面不出现占位式、试运行式产品文案。
- 未接入摄像头、物联网、监管信息源时明确显示连接状态。
- Dashboard 与健康评价页无 critical 级 WCAG 2 A/AA 问题。
- 运行时依赖执行 `npm audit --omit=dev` 后为 0 个已知漏洞。

## 视觉基线维护

只有在人工确认新界面正确后，才可更新基线：

```bash
npm run test:visual -- --update-snapshots
npm run test:visual
```

基线保存在 `e2e/product.spec.js-snapshots/`，测试报告保存在 `test-results/report/`。

